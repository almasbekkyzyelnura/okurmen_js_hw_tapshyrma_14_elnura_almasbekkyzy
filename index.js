

let counter = 0;
setInterval(function () {
    counter++; console.log(`Прошло: ${counter} сек`);
}, 1000);

    

/*
let counteer = 0;


    const interval = setInterval(function (){
      counteer++;
    console.log(`Прошло: ${counteer} сек`);
    if (counteer >= 5) {
    clearInterval(interval);
    }
    }, 1000);     */